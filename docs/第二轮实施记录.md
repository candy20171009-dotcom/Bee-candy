# BeeFintech 第二轮实施记录（2026-04-30）

## 本轮目标

基于第一轮 mock 闭环，完成可切换 Supabase 的数据层改造，并补充文档与可访问性细节。

## 已完成

1. 数据源配置升级
- 新增 `DATA_SOURCE` 环境变量解析：`mock | supabase`

2. Supabase 数据仓储实现
- 新增 `repositories/supabase/supabase-demo-request-repository.ts`
- 新增 `repositories/supabase/supabase-dividend-repository.ts`
- 通过 Supabase REST API 完成写入与查询

3. Repository 工厂切换逻辑
- `repositories/repository-factory.ts` 支持 mock/supabase 双实现注入

4. 可访问性增强
- 预约表单与查询筛选器补充可见 label 与 aria-label
- 输入提示语更明确，提升无障碍与可理解性

5. 文档更新
- `README.md` 增补 Phase 2 数据源切换说明

## 当前状态

- 在未配置 Supabase 环境变量时，默认仍走 mock 流程
- 配置 `DATA_SOURCE=supabase` 后可切换到真实数据源

## 下一步建议

1. 在 Supabase 建立 `demo_requests` 与 `dividend_rates` 表
2. 写入一批样例数据验证筛选条件
3. 增加 API 层自动化测试，覆盖 mock/supabase 双路径
